name: Deploy to Coolify

on:
  push:
    branches: [main]

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Backend
        run: |
          curl -s -X GET \
            -H "Authorization: Bearer ${{ secrets.COOLIFY_TOKEN }}" \
            "http://92.113.29.87:8000/api/v1/deploy?uuid=d11leh5adk10cjhbzuf0fzvq&force=false"
